require 'spec_helper'

describe Spree::Order, :type => :model do

  # note references spree[master] variant validation

  context "with addons" do
    it "should add errors if addon is destroyed" do
    end

    it "should add errors if addon on product is changed" do
    end
  end

end